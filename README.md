# seenHub-new-BE

Rails backend for SeenHub.

## Setup

1. `bundle install`
2. `yarn install`
3. `bin/rails db:create db:migrate`
4. `yarn build:css`
5. `bin/rails s`

## Rails Admin

Visit `/admin` after creating an admin user.

